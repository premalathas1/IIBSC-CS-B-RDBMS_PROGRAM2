- name: Configure MySQL
  run: |
    sudo mysql -e "CREATE DATABASE IF NOT EXISTS student_db;"
    sudo mysql -e "CREATE USER IF NOT EXISTS 'testuser'@'localhost' IDENTIFIED BY 'testpass';"
    sudo mysql -e "GRANT ALL PRIVILEGES ON student_db.* TO 'testuser'@'localhost';"
    sudo mysql -e "FLUSH PRIVILEGES;"
